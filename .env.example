# =============================================================================
# Client Pix - Environment Configuration
# =============================================================================
# This file documents all configurable environment variables.
#
# HOW TO USE:
# -----------
# - Coolify/Dokploy: Add these as environment variables in the platform UI
# - Self-hosted:     Copy to .env and edit: cp .env.example .env
#
# ONLY ONE REQUIRED VARIABLE: POSTGRES_PASSWORD
# Everything else has sensible defaults or is auto-generated!
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED
# -----------------------------------------------------------------------------

# Database password - use a strong random password
# Generate one with: openssl rand -base64 32
POSTGRES_PASSWORD=change-this-to-a-secure-password

# -----------------------------------------------------------------------------
# OPTIONAL - Domain (selfhost only)
# -----------------------------------------------------------------------------

# Your domain (without https://)
# Only needed for docker-compose.selfhost.yml - Coolify auto-detects this
# DOMAIN=photos.example.com

# -----------------------------------------------------------------------------
# OPTIONAL - Database
# -----------------------------------------------------------------------------

# Database username and name (defaults are fine for most users)
# POSTGRES_USER=clientpix
# POSTGRES_DB=clientpix

# -----------------------------------------------------------------------------
# OPTIONAL - Application
# -----------------------------------------------------------------------------

# Display name shown in the app
# APP_NAME=Client Pix

# Maximum dimension for web-optimized images (pixels)
# Higher = better quality but larger file sizes
# WEB_MAX_DIMENSION=2400

# -----------------------------------------------------------------------------
# OPTIONAL - Security
# -----------------------------------------------------------------------------

# JWT secret for authentication tokens
# If not set, a secure secret is auto-generated on first startup
# Generate your own with: openssl rand -base64 64
# JWT_SECRET=

# Enable debug mode (always use false in production)
# DEBUG=false
