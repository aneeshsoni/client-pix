[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13.6"
dependencies = [
    "fastapi>=0.128.0",
]

[dependency-groups]
dev = [
    "ruff>=0.13.1",
]
