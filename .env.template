# =============================================================================
# Client Pix / GalleryVault - Environment Variables Template
# =============================================================================
# Copy this file to .env and fill in the values for your environment.
# For Coolify: These variables will be available in the deployment settings.
#
# IMPORTANT: Never commit .env files with real credentials to git!
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------
APP_NAME="Client Pix"
DEBUG=false

# Base URL for the application (used for share links, etc.)
# In production, this should be your domain (e.g., https://gallery.yourdomain.com)
BASE_URL=http://localhost

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Format: postgresql+asyncpg://user:password@host:port/database
# For Coolify: The postgres container hostname is usually the service name
DATABASE_URL=postgresql+asyncpg://clientpix:CHANGE_THIS_PASSWORD@postgres:5432/clientpix

# PostgreSQL settings (for docker-compose)
POSTGRES_USER=clientpix
POSTGRES_PASSWORD=CHANGE_THIS_PASSWORD
POSTGRES_DB=clientpix

# -----------------------------------------------------------------------------
# Storage
# -----------------------------------------------------------------------------
# Directory where uploaded files are stored
# In Docker, this is typically a mounted volume
UPLOAD_DIR=/app/uploads

# Image processing settings
WEB_MAX_DIMENSION=2400

# -----------------------------------------------------------------------------
# Frontend (Next.js)
# -----------------------------------------------------------------------------
# API URL for the frontend to communicate with the backend
# In production with nginx, this should match BASE_URL
NEXT_PUBLIC_API_URL=http://localhost

# -----------------------------------------------------------------------------
# Security (Optional - for future authentication)
# -----------------------------------------------------------------------------
# JWT secret for signing tokens (generate with: openssl rand -hex 32)
# JWT_SECRET=your-secret-key-here

# Session secret (generate with: openssl rand -hex 32)
# SESSION_SECRET=your-session-secret-here

# -----------------------------------------------------------------------------
# CORS Settings
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins for CORS
# In production, set this to your domain
ALLOWED_ORIGINS=http://localhost,http://localhost:3000

# -----------------------------------------------------------------------------
# Coolify-Specific Notes
# -----------------------------------------------------------------------------
# When deploying to Coolify:
# 1. Set BASE_URL to your actual domain (e.g., https://gallery.yourdomain.com)
# 2. Set NEXT_PUBLIC_API_URL to the same value as BASE_URL
# 3. Generate secure passwords for POSTGRES_PASSWORD
# 4. Coolify will automatically handle SSL via Traefik
# 5. Make sure to set up persistent volumes for:
#    - PostgreSQL data (/var/lib/postgresql/data)
#    - Uploads directory (/app/uploads)
